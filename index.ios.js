/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 */
'use strict';
import React, {
  AppRegistry,
  Component,
} from 'react-native';

var Root = require('./app/Root');

AppRegistry.registerComponent('iota', () => Root);
